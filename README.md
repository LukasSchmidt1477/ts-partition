# Array partition

You need to split an array into two groups based on a predicate. Doing it in one pass saves cycles.

I built this with just the TypeScript standard library. Pulling in a massive npm package for basic array logic is a waste of build time. Zero dependencies to install or maintain.

```
partition.ts
```

Read the test file next to the source code for concrete examples of how it behaves.